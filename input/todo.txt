posi3 — TODO
============

Notes for Claude. Anything written here is read at the start of a session.


OPEN
----
- Adding the disguise model to the dashboard and connection
- Adding the Encoder Model to the dashboard and Encoder Config
- IP/Nic Picker in add connection optimieren
- Pop Layouts optimieren
- Dashboard stats rethink
- Web Interface reachable from other systems. Multi Ip reach.
- App and Web have different min width
- App name MacOs and Windows conistent, also in the OS Menus.
- Encoder Config Indicator Idle should be offline. Idle should be connected but not started? States for Encoders? Offline(off/unplugged) Connected(stopped) Sending(start)
- A1. DISGUISE MAPPING SCREEN still optimising
- A2. ZERO / PRESET 0 needs fix same thing.
- A3. RUN! (SINGLE SAMPLE) show only when Polled mode active. Where??
- A4. CONFIG EXPORT / IMPORT needs testing
- A5. LOG EXPORT -  needs testing
- A6. CONNECTION REORDERING - delete
- A7. STALE KEYS IN YOUR SAVED PROFILE - what even is this, prop remove completely
- A8. LOG-TO-FILE SETTING - WHAT? Where?

Still open and awaiting your call, unchanged: the desktop audit items (Zero/Preset 0 button, Run!, config export/import UI, connection reordering

DONE
----


REFERENCE
---------
Rig:      encoder 10.10.10.10 (fw 4.50) · encoder 10.10.10.20 · disguise 10.10.10.5
          this machine 10.10.10.2 on en3
Audit of inherited-but-never-requested features: ~/Desktop/posi3-inherited-features.txt
Full history and rationale: docs/FEATURES.md
