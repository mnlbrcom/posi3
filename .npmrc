# Keep every cache out of the Dropbox-synced project directory.
# Dropbox's file provider can lock files mid-write and produce a half-synced
# node_modules that builds a broken app.
cache=${HOME}/.npm
fund=false
audit=false
save-exact=true
